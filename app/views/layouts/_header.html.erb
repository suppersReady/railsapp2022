<header class="navbar-fixed">
  <nav class="blue darken-2">
    <div class="container nav-wrapper">
      <%=link_to "First App", root_path, class: "brand-logo" %>
      <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="material-icons">menu</i></a>
      <ul class="right hide-on-med-and-down">

          <% if utenti_signed_in? %>
            <li><%=link_to "Log Out", destroy_utenti_session_path, method: :destroy %></li>
          <% else %>  
            <li><%=link_to "Log In", new_utenti_session_path %></li>
            <li><%=link_to "Registrazione", new_utenti_registration_path %></li>
          <% end %>
      </ul>
      <ul class="sidenav" id="mobile-demo">
          <% if utenti_signed_in? %>
            <li><%=link_to "Log Out", destroy_utenti_session_path, method: :delete %></li>
          <% else %>  
            <li><%=link_to "Log In", new_utenti_session_path %></li>
            <li><%=link_to "Registrazione", new_utenti_registration_path %></li>
          <% end %>
      </ul>  
    </div>
  </nav>
</header>  